<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	tools:context="tanawinwichitcom.android.mooglemobile.MainActivity">

	<android.support.v7.widget.RecyclerView
		android:id="@+id/movieRecyclerView"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:fastScrollEnabled="true"
		app:layout_constraintBottom_toBottomOf="@+id/loadProgress"
		app:layout_constraintEnd_toEndOf="@+id/loadProgress"
		app:layout_constraintHorizontal_bias="0.0"
		app:layout_constraintStart_toStartOf="@+id/loadProgress"
		app:layout_constraintTop_toTopOf="@+id/loadProgress">
	</android.support.v7.widget.RecyclerView>

	<ProgressBar
		android:id="@+id/loadProgress"
		style="@android:style/Widget.DeviceDefault.Light.ProgressBar"
		android:layout_width="wrap_content"
		android:foregroundGravity="center"
		android:layout_height="wrap_content"
		android:layout_centerInParent="true"/>

	<TextView
		android:id="@+id/loadDetail"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:layout_centerHorizontal="true"
		android:layout_below="@id/loadProgress"/>

</RelativeLayout>
